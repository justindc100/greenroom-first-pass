{
  "schemaVersion": "1.0",
  "classification": "test",
  "isolated": true,
  "resetStrategy": "ephemeral_deployment",
  "networkControl": "playwright",
  "allowedHosts": ["127.0.0.1"],
  "productionHosts": [],
  "sandboxPurchases": false,
  "notes": "Throwaway first-pass repository: a self-contained static app served inside the CI job. Nothing outside the job is reachable."
}
